# Step 2 — 이름 월드컵

## 개요
Step 1에서 생성된 이름들을 "현재 우승자 vs 새 도전자" 방식으로 1:1 대결시켜 최종 이름 하나를 결정한다.  
토너먼트 방식이 아니라 **누적 챔피언 방식** (현재 1위가 도전자를 계속 이기면 살아남는 구조).

---

## 월드컵 진행 방식

```
초기 이름 목록: [A, B, C, D, E ...]

라운드 1: 현재 챔피언(A) vs 도전자(B)
  → 사용자가 선택
  → 선택된 이름이 다음 챔피언

라운드 2: 챔피언(승자) vs 도전자(C)
  → ...

마지막 라운드: 챔피언 vs 마지막 도전자
  → 최종 우승자 확정
```

- 진행률: `현재 라운드 / 전체 이름 수 - 1` × 100%
- 전체 이름이 N개면 N-1번의 대결

---

## 화면 구성

### 월드컵 진행 화면
```
┌─────────────────────────────────────────┐
│  이름 월드컵                            │
│  진행률 ████████░░░░░░░  8 / 19        │
├──────────────┬──────────────────────────┤
│              │                          │
│   현재 챔피언 │    새 도전자             │
│              │                          │
│    지 우     │     서 윤               │
│   (智宇)     │    (瑞允)               │
│              │                          │
│ 지혜롭고 넓은│ 상서로움과 공정함        │
│ 우주를 품는  │                          │
│              │                          │
│  [← 지우 선택] [서윤 선택 →]           │
│                                         │
│           [처음부터 다시]               │
└─────────────────────────────────────────┘
```

### 최종 결과 화면
```
┌─────────────────────────────────────────┐
│  🏆 최종 우승 이름                      │
│                                         │
│          김 지 우 (金 智 宇)            │
│                                         │
│  뜻: 지혜롭고 넓은 우주를 품는다는 뜻  │
│                                         │
│  ── 이름 희귀도 ──                     │
│  현재 "지우"를 사용하는 인구: 약 12,400명│
│  희귀도: 보통 (상위 15% 수준)           │
│                                         │
│  이 이름을 저장하시겠습니까?            │
│  [✅ 저장하기]   [❌ 저장 안 함]        │
└─────────────────────────────────────────┘
```

---

## 이름 희귀도 안내

실시간 통계 DB 대신 AI가 추정값을 제공한다.

**프롬프트:**
```
한국 이름 "{name}"에 대해 다음을 추정해 주세요:
1. 현재 한국에서 이 이름을 사용하는 인구 (통계청 2015년 기준 추정)
2. 희귀도 등급: 매우 희귀 / 희귀 / 보통 / 흔한 편 / 매우 흔함

JSON으로만 반환:
{"estimated_population": 숫자, "rarity": "등급", "rarity_description": "설명 1문장"}
```

---

## 백엔드 API

### `POST /api/worldcup/rarity`
최종 우승 이름의 희귀도 조회.

**Request:**
```json
{ "name": "지우", "gender": "남아" }
```

**Response:**
```json
{
  "estimated_population": 12400,
  "rarity": "보통",
  "rarity_description": "2015년 기준 약 12,400명이 사용하며 흔하지도 희귀하지도 않은 이름입니다."
}
```

---

## 프론트엔드 상태 관리

```javascript
// 월드컵 상태 (localStorage 또는 메모리)
worldcupState = {
  names: [...],        // 전체 이름 배열
  champion: null,      // 현재 챔피언
  challengerIndex: 1,  // 현재 도전자 인덱스
  totalRounds: N - 1,
  currentRound: 0,
  criteria: {...}      // Step 1에서 설정한 기준 (카드에 표시용)
}
```

- Step 1 결과 화면에 **[→ 월드컵 시작]** 버튼 추가
- 월드컵 진행 중 새로고침해도 복원되도록 `localStorage`에 상태 저장

---

## 저장 데이터 구조

```json
{
  "id": "uuid",
  "name": "지우",
  "hanja": "智宇",
  "meaning": "...",
  "gender": "남아",
  "last_name": "김",
  "full_name": "김지우",
  "rarity": "보통",
  "estimated_population": 12400,
  "saved_at": "2026-05-09T12:00:00",
  "criteria_used": ["유행을 덜 타야 함", "발음이 자연스러워야 함"]
}
```

저장소: `localStorage["saved_names"]` (JSON 배열)

---

## 구현 순서
1. Step 1 결과 화면에 "월드컵 시작" 버튼 추가
2. 월드컵 화면 컴포넌트 구현 (챔피언 vs 도전자 카드)
3. 진행률 바 구현
4. `POST /api/worldcup/rarity` 엔드포인트 구현
5. 최종 결과 + 저장 기능 구현
