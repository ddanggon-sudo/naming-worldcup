# Step 3 — 왕중왕전

## 개요
Step 2에서 저장된 이름들을 모아 다시 한 번 "현재 챔피언 vs 새 도전자" 방식으로 최종 1개의 이름을 결정한다.  
저장된 이름이 2개 이상일 때만 진행 가능하다.

---

## Step 2와의 차이점

| 항목 | Step 2 (이름 월드컵) | Step 3 (왕중왕전) |
|------|---------------------|------------------|
| 이름 출처 | Step 1 생성 결과 | localStorage 저장 이름 전체 |
| 기준 적용 | Step 1 기준 카드에 표시 | 각 이름에 저장된 기준 표시 |
| 목적 | 한 세션의 최종 이름 선택 | 여러 세션에 걸쳐 저장된 후보 중 최종 결정 |
| 진입 경로 | Step 1 결과 → 월드컵 시작 | 메인 네비게이션 → 왕중왕전 |

---

## 화면 구성

### 왕중왕전 진입 화면
```
┌─────────────────────────────────────────┐
│  👑 왕중왕전                            │
│                                         │
│  저장된 이름 7개로 최종 결정을 내립니다 │
│                                         │
│  저장된 이름 목록:                      │
│  • 김지우 (智宇) — 2026-05-01 저장     │
│  • 김서준 (書俊) — 2026-05-03 저장     │
│  • 김도윤 (道允) — 2026-05-05 저장     │
│  • ...                                  │
│                                         │
│         [👑 왕중왕전 시작하기]          │
│                                         │
│  ⚠ 저장된 이름이 1개 이하면 비활성화  │
└─────────────────────────────────────────┘
```

### 대결 화면
Step 2와 동일한 레이아웃 사용.  
카드에 **저장 날짜**와 **그 이름을 선택했을 때의 기준** 함께 표시.

```
┌──────────────┬──────────────────────────┤
│  현재 챔피언  │    새 도전자             │
│   김지우      │    김서준               │
│   (智宇)      │   (書俊)               │
│              │                          │
│ 저장: 5/1    │  저장: 5/3              │
│ 기준: 부드러운│  기준: 똑똑한 느낌      │
│              │                          │
│ [← 지우 선택] │ [서준 선택 →]          │
└─────────────────────────────────────────┘
```

### 최종 결과 화면
Step 2 최종 결과와 동일 구조.  
"저장하기" 대신 **"이 이름으로 최종 확정"** 버튼.  
최종 확정 시 해당 이름에 `is_final: true` 플래그 추가.

```
┌─────────────────────────────────────────┐
│  👑 왕중왕전 최종 우승                  │
│                                         │
│          김 지 우 (金 智 宇)            │
│                                         │
│  ── 이름 희귀도 ──                     │
│  현재 "지우"를 사용하는 인구: 약 12,400명│
│  희귀도: 보통 (상위 15% 수준)           │
│                                         │
│  [✅ 이 이름으로 최종 확정]             │
│  [↩ 왕중왕전 다시 하기]                │
└─────────────────────────────────────────┘
```

---

## 백엔드 API

Step 2의 `POST /api/worldcup/rarity` 재사용.  
추가 엔드포인트 없음 — 저장/조회는 모두 `localStorage`에서 처리.

---

## 프론트엔드 상태 관리

```javascript
// 왕중왕전 초기화
function initGrandTournament() {
  const saved = JSON.parse(localStorage.getItem("saved_names") || "[]");
  if (saved.length < 2) {
    alert("왕중왕전을 하려면 저장된 이름이 2개 이상 필요합니다.");
    return;
  }
  // Step 2와 동일한 worldcupState 구조로 시작
  worldcupState = {
    names: saved,
    champion: saved[0],
    challengerIndex: 1,
    totalRounds: saved.length - 1,
    currentRound: 0,
    mode: "grand"   // 왕중왕전 모드 구분용
  };
}
```

---

## 최종 확정 처리

```javascript
function finalizeWinner(winner) {
  const saved = JSON.parse(localStorage.getItem("saved_names") || "[]");
  const updated = saved.map(n => ({
    ...n,
    is_final: n.id === winner.id
  }));
  localStorage.setItem("saved_names", JSON.stringify(updated));
}
```

---

## 구현 순서
1. 네비게이션에 "왕중왕전" 메뉴 추가
2. 왕중왕전 진입 화면 (저장 이름 목록 + 시작 버튼) 구현
3. Step 2 월드컵 로직을 `mode` 파라미터로 재사용
4. 최종 확정 버튼 및 `is_final` 플래그 처리
5. 희귀도 조회 (Step 2 API 재사용)
